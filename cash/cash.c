#include <cs50.h>
#include <stdio.h>

#define A, B, C, D 1, 5, 10, 25

int main(void)
{
    int valor;
    do
    {
        valor = get_int("Change owed: ");
    }
    while (valor < 0);

    int a, b, c, d;

    if
}
