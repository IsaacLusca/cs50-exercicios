#include <cs50.h>
#include <ctype.h>
#include <string.h>
#include <stdio.h>

int main(void)
{
    int x = 0;
    string texto[x] = get_string("Text: ");

    if (texto[x] == ' ')
    {
        total = t - 1;
    }
    else
    {
        int t = strlen(texto);
    }

    printf("%i\n", t);
}

//

//
